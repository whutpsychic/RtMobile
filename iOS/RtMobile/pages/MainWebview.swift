// 主显示页

import SwiftUI

struct MainWebview: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("《MainWebview》")
        }
        .padding()
    }
}

#Preview {
    MainWebview()
}
