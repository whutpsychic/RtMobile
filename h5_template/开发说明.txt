# 开发说明

## 【 服务器地址 】
 - 如果您需要配置服务器地址，请关注/public/serverConfig.js。
 - 并结合/src/utils/index#getURLByType方法配合修改。

## 【 关于开发和调试 】
 - 使用@Compose开发模式的框架使内置浏览器不支持vh计量单位，所以预置了自定义的变量 --vh 用以兼容这种情况。而 xml+kotlin 开发模式则不会发生这种情况。
 - 在某些PDA的老旧机型中，@Compose开发模式不仅无法正常响应 npm run dev ，得用 npm run preview 预览效果；而且还可能发生使得html当中的某些元素无法正常显示。所以目前来看还是xml+kotlin这种传统开发模式效果更稳定。

## 【 关于原生接口与地址配置 】
 - 当您看到瑞太logo时点击logo图标即可跳转至地址配置页，配置的地址长期有效。
 - 点击《登录页》上方的标题可跳转至《开发者演示页》，里面放置了所有的原生接口演示项。
 - 桥接包为 rtlink-mbridge。

## 【 rtlink-mbridge 使用说明 】

// 【 获取设备信息 】
 - getDeviceInfo()
// -------------- iOS / Android --------------
// "deviceId": 设备唯一标识符,
// "deviceName": 设备名称,
// "systemName": 系统名称,
// "systemVersion": 系统版本号,
// "modelDisplayName": 设备型号名称,
// "screenWidth": 屏幕宽度,
// "screenHeight": 屏幕高度,
// "orientation": 当前屏幕方向（横向/竖向）,

// 【 写入本地缓存 】
 - writeLocal(key: string, value: string, seconds?: number)
// -------------- iOS / Android --------------
// 参数说明:
//  - key: 缓存键
//  - value: 缓存值
//  - seconds: 有效期默认为99年


// 【 读取本地缓存 】
 - readLocal(key: string)
// -------------- iOS / Android --------------
// 参数说明:
//  - key: 缓存键
// 如果数据过期或不存在则读取结果为""


// 【 拨打电话 】
 - preDial(number: string)
// 参数说明:
//  - number: 电话号

// 【 检查网络连接 】
 - checkoutNetwork()
// -------------- iOS --------------
// 可能得到的结果:
// ['Wifi', 'Cellular', 'Ethernet', 'Loopback', 'Connected', 'Disconnected']
// -------------- Android --------------
// 可能得到的结果:
// ['WIFI', 'MOBILE', 'BLUETOOTH', 'ETHERNET', 'VPN', 'null']


// 【 混合扫码 】
 - scan()
// -------------- iOS / Android --------------
// 支持的码类型
// .qr,
// .code128,
// .code39,
// .code93,
// .pdf417,
// .ean13,
// .ean8,
// .upce,
// .dataMatrix
