// url配置页

import SwiftUI

struct UrlConfig: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("《UrlConfig》")
        }
        .padding()
    }
}

#Preview {
    UrlConfig()
}
